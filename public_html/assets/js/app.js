document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('[data-menu-toggle]').forEach(btn=>{btn.addEventListener('click',()=>{const target=document.querySelector(btn.dataset.menuToggle);if(target)target.classList.toggle('is-open')})});document.querySelectorAll('.flash-close').forEach(btn=>btn.addEventListener('click',()=>btn.closest('.flash')?.remove()));document.querySelectorAll('[data-auto-dismiss]').forEach(el=>{const ms=Number(el.dataset.autoDismiss)||5000;setTimeout(()=>el.remove(),ms)});document.querySelectorAll('form[data-loading]').forEach(form=>form.addEventListener('submit',()=>{const submit=form.querySelector('button[type=submit],button:not([type])');if(submit){submit.disabled=true;submit.dataset.originalText=submit.textContent;submit.textContent='Please wait…'}form.classList.add('loading')}));document.querySelectorAll('[data-confirm]').forEach(el=>el.addEventListener('click',e=>{if(!window.confirm(el.dataset.confirm)){e.preventDefault();e.stopPropagation()}}));document.querySelectorAll('[data-modal-open]').forEach(el=>el.addEventListener('click',()=>document.querySelector(el.dataset.modalOpen)?.classList.add('is-open')));document.querySelectorAll('[data-modal-close]').forEach(el=>el.addEventListener('click',()=>el.closest('.modal-backdrop')?.classList.remove('is-open')));document.querySelectorAll('.modal-backdrop').forEach(el=>el.addEventListener('click',e=>{if(e.target===el)el.classList.remove('is-open')}))});